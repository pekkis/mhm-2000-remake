import type { DefensivePairing, ForwardLine } from "@/state/lineup";
import type { FC } from "react";

type Props = {
  line: ForwardLine;
  defensivePairing?: DefensivePairing;
};

export const LineView: FC<Props> = () => {
  return "lain";
};
